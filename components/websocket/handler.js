
import { queryClient } from "../../contexts/query-client";

export default function WebsocketHandler(data) {
    const messageRecipients = {
        home: '',
        convos: '',
        convo: '',
        notes: '', 
        note: '',
        lists: '',
        list: '',
      };
}